第一个参数为基本流文件名;
第二个参数为工作方式
如果工作方式为0
则
第三个参数是存yuv数据的帧起始;
第四个参数是存yuv数据的帧结束;
输出文件为当前目录下的test.yuv
如果第四个参数小于等于第三个参数则不输出文件只解码
如果工作方式为1
则
第三个参数是显示窗口的宽度;
第四个参数是显示窗口的高度;

./TestAVSDecoder32.x 1.es 0 0 0
./TestAVSDecoder32.x 1.es 1 400 300

截图由于采用SDL的Overlay无法截到视频图。
只有32位程序才支持SDL显示

